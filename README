cronch is a compression algorithm.

cronch compresses a file by using the input stream to generate an artificial neural network that will generate the contents of the input stream when run by the decompression algorithm.
